# Build output (regenerated by `next build` / `next dev`, never commit)
.next/
out/

# Local env files (never commit secrets)
.env*.local

# Vercel CLI
.vercel

# Editor/OS cruft
.DS_Store
*.tsbuildinfo
